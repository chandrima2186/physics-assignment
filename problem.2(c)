#include<stdio.h>
#include<math.h>
int main()
{
    //tht means theta,which is the angle
    double tht = 45 *M_PI / 180;
    double sin_theta = sin(tht);
    double ratio = 1/ sin_theta ;
    printf("The ratio of slit width ot wavelength is : %.2f\n",ratio);
    return 0;
}
